# Generated by ChatGPT 4o

services:
  gitlo-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"  # localhost to container port
    volumes:
      - ./app:/app   # hot reload with live file sync
    environment:
      - HOST=0.0.0.0 # required so Nuxt.js listens on all interfaces
    command: pnpm dev
